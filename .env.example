# ============================================================
# Client-Side Configuration (Bundled into Vite / Android APK)
# ============================================================
VITE_FIREBASE_API_KEY=
VITE_ADMIN_EMAIL=
VITE_ADMIN_DISPLAY_NAME=
VITE_BACKEND_URL=
VITE_HOSTING_URL=

# ============================================================
# Server-Side Configuration ONLY (NEVER prefix with VITE_)
# Put these in a local .env file. They are NEVER bundled into APK.
# ============================================================
GEMINI_API_KEY=
PORT=3000
# GOOGLE_APPLICATION_CREDENTIALS=
